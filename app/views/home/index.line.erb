<%= raw(
  Kamiflex.build(self) do
    alt_text "服務名稱傳送了一則訊息"
    bubble do
      body do
        flex_nav
        text "歡迎使用服務名稱", wrap: true, size: :sm, margin: :lg
        horizontal_box margin: :lg do
          message_button "關於我們", "關於我們", style: :primary, margin: :md
        end
      end
    end
  end
)%>