<h1>
  <%= link_to 服務名稱, "/" %>
  <% if current_user.present? %>
    <%= current_user.name %> 您好：
    <%= button_to "登出", destroy_user_session_path, method: :delete, form_class: 'inline-form', style: "display: inline; background: none; border: none; padding: 0; font: inherit; cursor: pointer; color: #069; text-decoration: underline;" %>
  <% else %>
    <%= button_to "登入", user_line_omniauth_authorize_path, data: {turbo: false}, form_class: 'inline-form', style: "display: inline; background: none; border: none; padding: 0; font: inherit; cursor: pointer; color: #069; text-decoration: underline;" %>
  <% end %>
</h1>
<hr />